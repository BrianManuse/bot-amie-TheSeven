const MedoAPI = require('./index');

let games = [{
    'Joue à': 'PLAYING',
    'Regarde': 3,
    'Ecoute': 'LISTENING',
}];

let api_key = "VOTRE CLE D'API (.api sur Discord.MedoDev.xyz)";
let token = "VOTRE TOKEN DE BOT DISCORD (sur https://discord.com/developers)";
let game_name = "NOM DU JEU QUE VOUS VOULEZ METTRE [optionnel]";

let jeux = false; // Mettez true si vous voulez mettre un jeux

// Mettre en ligne un bot

MedoAPI.onlineBot(api_key, token, (jeux == true ? [games[0/*Si vous voulez qu'il fasse "Joue à" (pas tous disponibles)*/], game_name] : null));

// Mettre à jour un bot (si il a été mis en ligne avec l'API)

MedoAPI.updateBot(api_key, token, (jeux == true ? [games[0/*Si vous voulez qu'il fasse "Joue à" (pas tous disponibles)*/], game_name] : null));
